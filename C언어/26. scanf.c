#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main(void) {

    // 선언
    int a;
    float b;
    char c;

    // 입력
    scanf("%d, %f, %c", &a, &b, &c);

    printf("%d, %f, %c \n", a, b, c);

    return 0;

}